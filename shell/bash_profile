# ~/.bash_profile - Login shell configuration
# This file is executed for login shells

# Source .bashrc if it exists
if [ -f "$HOME/.bashrc" ]; then
    source "$HOME/.bashrc"
fi

# Add any login-specific configuration below
# (Environment variables that should only be set once)

# Added by LM Studio CLI tool (lms)
export PATH="$PATH:/home/rich/.lmstudio/bin"
. "$HOME/.cargo/env"

. "$HOME/.local/share/../bin/env"


# Added by Antigravity CLI installer
export PATH="/home/rich/.local/bin:$PATH"
